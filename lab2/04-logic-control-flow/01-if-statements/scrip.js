// Cú pháp câu lệnh if
if (điều_kiện) {
    // Mã thực thi nếu điều_kiện là true
  } else {
    // Mã thực thi nếu điều_kiện là false
  }
  
  // Ví dụ 1: Câu lệnh if đơn giản
  if (true) {
    console.log('Điều này đúng');
  }
  
  if (false) {
    console.log('Điều này KHÔNG đúng');
  }
  
  // Ví dụ 2: Đánh giá biểu thức
  const x = 10;
  const y = 5;
  
  if (x >= y) {
    console.log(`${x} lớn hơn hoặc bằng ${y}`);
  }
  
  if (x === y) {
    console.log(`${x} bằng ${y}`);
  } else {
    console.log(`${x} KHÔNG bằng ${y}`);
  }

  